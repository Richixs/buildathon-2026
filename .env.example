DATABASE_URL="postgresql://pollar_track:pollar_track@localhost:5432/pollar_track?schema=public"

# Reown AppKit project ID — create one at https://cloud.reown.com
NEXT_PUBLIC_PROJECT_ID=""

# Wallet that custodies pledged HSK until a real escrow contract exists.
# InvestForm sends native HSK here via wagmi's useSendTransaction — no
# Solidity/escrow yet, see EQUITY_CHAIN_HANDOFF.md gap #1.
NEXT_PUBLIC_TREASURY_ADDRESS=""
